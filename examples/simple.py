# This a mockup of what the python file could look like

"""AUTO GENERATED PYTHON FILE BY PROJECT2"""

import simple

# def construct_Rocket(max_speed: float, price: int, number_of_engines: int) -> simple.Rocket:
#     return simple.Rocket(max_speed, price, number_of_engines)

# def launch(r: simple.Rocket, when: str, where: str) -> None:
#     return simple.launch(r, when, where)

print(simple.hello())