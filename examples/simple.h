struct Rocket {
    double       max_speed;
    long         price;
    unsigned int number_of_engines;
};

inline void launch(Rocket r, char const* when, char const* where);

int defined() {
    return 0;
}
